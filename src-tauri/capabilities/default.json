{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main ContractorCRM window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
